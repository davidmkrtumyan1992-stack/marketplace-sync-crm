Прочитай CLAUDE.md и SPECIFICATION.md для контекста.

Нужно зафиксировать «золотой стандарт» работы аналитики Ozon
в документации чтобы при подключении WB и Яндекс ничего не сломалось.

ШАГ 1 — Обновить CLAUDE.md:

В раздел "Что НЕЛЬЗЯ трогать без явного указания" добавь:

### Синхронизация заказов Ozon — Золотой стандарт:
- since = предыдущий день 21:00:00 UTC (= 00:00:00 МСК текущего дня)
- to = текущий день 21:00:00 UTC (= 00:00:00 МСК следующего дня)
- FBO limit: 1000 (НЕ 50!)
- FBS limit: 1000
- FBO цены берутся из financial_data (НЕ из каталога товара)
- Все fetch к api-seller.ozon.ru через fetchWithRetry (3 попытки)
- Пагинация максимум 10 страниц на магазин

### Логика дашборда — Двойные показатели:
- grossRevenue = ВСЕ заказы включая cancelled (= "Заказано" в Ozon)
- netRevenue = только не отменённые (= реальная выручка)
- getSalesData в storage.ts возвращает ОБА показателя
- График показывает две линии: синяя (gross) + зелёная (net)
- Логика универсальна для ВСЕХ маркетплейсов — не переписывать!

### Даты в БД:
- created_at хранится в UTC
- Для отображения всегда конвертировать:
  DATE(created_at AT TIME ZONE 'UTC' AT TIME ZONE 'Europe/Moscow')
- НЕ использовать DATE(created_at) без конвертации!

ШАГ 2 — Обновить SPECIFICATION.md:

В раздел 6 "Статус задач" секцию "✅ Завершено" добавь:

✓ Золотой стандарт синхронизации Ozon (UTC→МСК, limit 1000, financial_data)
✓ Двойные показатели выручки (gross/net) на дашборде
✓ fetchWithRetry для всех 26 вызовов Ozon API
✓ Пагинация FBO/FBS (макс 10 страниц × 1000 = 10 000 заказов)
✓ Исторический ресинк март 2026 (1756 заказов с правильными данными)
✓ Синхронизация данных совпадает с кабинетом Ozon по всем 5 магазинам
✓ Защита от поломки при добавлении WB и Яндекс задокументирована

В раздел 5 "Маркетплейсы" добавь подраздел 5.4:

### 5.4 Правила при подключении новых маркетплейсов

ВАЖНО: при подключении WB и Яндекс соблюдать эти правила:

WB (Wildberries):
- Добавить sync-orders по аналогии с Ozon
- since/to: тот же принцип UTC→МСК (предыдущий день 21:00 UTC)
- Добавить wbStatus поле в orders таблицу
- getSalesData уже поддерживает WB — НЕ переписывать!
- Добавить только фильтрацию отменённых для WB статусов

Яндекс Маркет:
- Синхронизация заказов уже работает ✓
- yandexStatus='CANCELLED' и 'RETURNED' уже фильтруются ✓
- grossRevenue уже включает Яндекс заказы ✓
- При подключении новых магазинов — только добавить в marketplace_settings
- getSalesData НЕ трогать!

Общий принцип:
getSalesData в storage.ts универсальная для всех МП.
При добавлении нового МП добавлять только:
1. Синхронизацию заказов (новый эндпоинт sync-orders)
2. Маппинг статусов отмены в фильтр getSalesData
3. НЕ переписывать getSalesData целиком!

ШАГ 3 — Добавить защитные комментарии в код:

В server/storage.ts найди метод getSalesData.
Добавь комментарий в самое начало метода:

/**
 * ЗОЛОТОЙ СТАНДАРТ — НЕ ИЗМЕНЯТЬ БЕЗ ВЕСКОЙ ПРИЧИНЫ
 *
 * grossRevenue = ВСЕ заказы включая отменённые (= "Заказано" в Ozon)
 * netRevenue = только активные заказы (= реальная выручка продавца)
 *
 * Логика универсальна для всех МП (Ozon, WB, Яндекс).
 * При добавлении нового МП — добавить его статус отмены в фильтр ниже.
 * Текущие статусы отмены:
 * - Ozon: status='cancelled' OR ozon_status='cancelled'
 * - Яндекс: yandex_status='CANCELLED' OR yandex_status='RETURNED'
 * - WB: добавить когда подключат
 */

В server/routes.ts найди POST /api/marketplace/ozon/sync-orders.
Добавь комментарий в самое начало:

/**
 * ЗОЛОТОЙ СТАНДАРТ OZON СИНХРОНИЗАЦИИ — НЕ ИЗМЕНЯТЬ БЕЗ ВЕСКОЙ ПРИЧИНЫ
 *
 * Временная зона: since = предыдущий день 21:00 UTC = 00:00 МСК
 * FBO limit: 1000 (было 50 — не возвращать!)
 * FBO цены: из financial_data (не из каталога)
 * Retry: fetchWithRetry (3 попытки при 429/502/504)
 * Пагинация: макс 10 страниц × 1000 = 10 000 заказов на магазин
 */