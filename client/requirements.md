## Packages
recharts | Dashboard analytics charts
@tanstack/react-table | Advanced data tables for inventory and orders
date-fns | Date formatting for orders and logs

## Notes
Authentication is handled via /api/login (Replit Auth).
API calls should use credentials: "include".
Dashboard stats are derived from list endpoints for MVP.
